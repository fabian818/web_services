<%= form_for(@my_action) do |f| %>
  <% if @my_action.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@my_action.errors.count, "error") %> prohibited this my_action from being saved:</h2>

      <ul>
      <% @my_action.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :url %><br>
    <%= f.text_field :url %>
  </div>
  <div class="field">
    <%= f.label :active %><br>
    <%= f.check_box :active %>
  </div>
  <div class="field">
    <%= f.label :provider_id %><br>
    <%= f.select :provider_id, options_from_collection_for_select(@providers, :id, :name) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
